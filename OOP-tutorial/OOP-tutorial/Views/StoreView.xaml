<UserControl x:Class="OOP_tutorial.Views.StoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OOP_tutorial.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding Path=StoreModule, Source={StaticResource Locator}}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Grid.ColumnSpan="2" Margin="10">
            <TextBlock Text="List of Things in the Store"                   
                   HorizontalAlignment="Center"
                   FontSize="20"></TextBlock>
            <StackPanel Orientation="Horizontal">
                <CheckBox Content="All" Margin="10,0"></CheckBox>
                <CheckBox Content="Book" Margin="10,0"></CheckBox>
                <CheckBox Content="Souvenir" Margin="10,0"></CheckBox>
                <CheckBox Content="Jewelry" Margin="10,0"></CheckBox>
            </StackPanel>
        </StackPanel>
        
        <ListBox Grid.Row="1"
                 Grid.Column="0"
                 Grid.RowSpan="4"
                 Margin="10"
                 ItemsSource="{Binding Path=Things, UpdateSourceTrigger=PropertyChanged}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Path=Name}"></TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
